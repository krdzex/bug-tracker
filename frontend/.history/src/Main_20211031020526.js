import React from 'react';
import { BrowserRouter } from 'react-router-dom';

const Main = () => {
    return (
        <BrowserRouter>
        <div className></div>
        </BrowserRouter>
    );
};

export default Main;